import { ClassificacaoToxicologica } from './classificacao-toxicologica.model';

describe('ClassificacaoToxicologica', () => {
  it('should create an instance', () => {
    expect(new ClassificacaoToxicologica()).toBeTruthy();
  });
});
